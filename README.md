# PhosphorOS

A fake terminal styled like an old CRT monitor, in one self-contained HTML file. No dependencies.

Open `phosphor-shell.html` in a browser. Type `help` for commands, `fx` to toggle the effect layers, `theme amber` to retint the tube.

`prompt.md` is a build prompt that regenerates the whole demo from scratch.
